---
title: MDX Test File
author: German
tags: [nvim, mdx, test]
---

# MDX Test Document

This is the first pre-section paragraph (`¶1`). The H1 above is ignored
entirely for `§` counting; the title belongs in frontmatter.

A second pre-section paragraph (`¶2`).

> A regular blockquote in the pre-section region counts as its own block —
> the gutter on the first line of this quote should read `¶3`.

> Mental Note: this scratchpad is skipped entirely. The gutter stays blank
> here and the counter does not advance.

The paragraph right after the Mental Note is `¶4`, not `¶5`. Scratchpads
consume no number.

## Prose and inline formatting

The H2 line above renders as `§1`. This paragraph is `§1¶1`. Inline
formatting — *italic*, **bold**, ***both***, ~~strike~~, `inline code`,
[links](https://mdxjs.com) — does not affect counting.

Soft-wrapped prose stays one block: `§1¶2`. Multiple physical lines between
blank-line boundaries collapse into a single ¶, so `<leader>w` can rewrap
to 80 columns without renumbering anything.

> A non-scratchpad blockquote is `§1¶3`. Its first token is not one of the
> scratchpad markers, so it counts as a real prose block.
>
> Second physical paragraph inside the same blockquote — still part of the
> same ¶, since the whole blockquote is one block.

> TODO: scratchpad markers aren't just `Mental Note` — `TODO`, `Note to
> self`, and `Draft note` all suppress the count too.

Paragraph right after the TODO scratchpad: `§1¶4`.

### Nested H3 inside §1

Entering an H3 opens a new scope, so the gutter on the line above shows
`§1.1` and `¶` resets to `0`. This paragraph is `§1.1¶1`, **not** `§1¶5`.

A second paragraph inside `§1.1` (`§1.1¶2`). Crossing any heading — H2 or
deeper — resets `¶`.

#### Nested H4 inside §1.1

The H4 line above is `§1.1.1`. This paragraph is `§1.1.1¶1`.

##### Nested H5 inside §1.1.1

The H5 line above is `§1.1.1.1`. This paragraph is `§1.1.1.1¶1`.

###### Nested H6 (the deepest level)

The H6 line above is `§1.1.1.1.1`. This paragraph is `§1.1.1.1.1¶1`. H6 is
the deepest level that contributes to the path.

### Second H3 sibling of §1.1

The H3 line above is `§1.2` — its sibling index advanced from `.1` to `.2`
because we're still under the same H2 parent. This paragraph is `§1.2¶1`.

> Mental Note: scratchpads inside a nested section still skip, and the
> next paragraph keeps the counter going as usual.

Paragraph after the scratchpad (`§1.2¶2`, also addressable as
`§1.2, post-scratchpad`).

## Lists and continuation rules

Section intro — `§2¶1`. The next paragraph flows directly into a list
with no blank line separating them:
- Continuation item a
- Continuation item b
  - Nested item
  - Another nested
- Continuation item c

The list above belongs to `§2¶2` (no preceding blank line). This
paragraph is `§2¶3`.

- Standalone list item a
- Standalone list item b
- Standalone list item c

A list separated by blanks on both sides is its own ¶: above is `§2¶4`,
this paragraph is `§2¶5`.

1. Ordered first
2. Ordered second
3. Ordered third

Ordered list above is `§2¶6`; this is `§2¶7`.

- [x] Completed task
- [ ] Pending task
- [ ] Another pending

Task list above is `§2¶8`; this is `§2¶9`.

## Tables, code, math

Section intro — `§3¶1`. Tables, fenced code, and block math all count as
one ¶ each.

| Feature          | Status | Notes                      |
| ---------------- | :----: | -------------------------- |
| Filetype         |   ✓    | `mdx` ext → `mdx` ft       |
| Treesitter       |   ✓    | reuses `markdown` parser   |
| LSP              |   ?    | requires `mdx_analyzer`    |
| Inline render    |   ✓    | `render-markdown.nvim`     |

Table above is `§3¶2`. This paragraph is `§3¶3`.

```ts
type Props = { label: string; onClick: () => void }

export function Button({ label, onClick }: Props) {
  return <button onClick={onClick}>{label}</button>
}
```

TS fence above is `§3¶4`. Blank lines inside the fence don't break it.
This paragraph is `§3¶5`.

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

Python fence above is `§3¶6`. This paragraph is `§3¶7`.

Block math below is `§3¶8`:

$$
\int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}
$$

The display equation above (lines `$$ … $$`) is `§3¶9`. This paragraph is
`§3¶10`.

### Inline things stay in their host block

Section intro for `§3.1¶1`. Inline math `$E = mc^2$`, inline JSX
`{new Date().getFullYear()}`, and a footnote reference[^1] all stay inside
this paragraph — they don't open new blocks.

[^1]: Footnote definitions are their own prose block — this line is
  `§3.1¶2`.

## Scratchpad showcase

Section intro — `§4¶1`. The four scratchpad markers below are all skipped
from counting.

> Mental Note: dot-com era example would belong here.

> TODO: outline the structural critique before the rewrite.

> Note to self: cite the original blog post, not the summary.

> Draft note: this header text needs a sharper verb.

None of those four scratchpads incremented ¶. This paragraph is `§4¶2`.

> Mental Note: adjacent scratchpads still skip.
> A lazy continuation line without `>` stays part of the scratchpad too.

Paragraph after the scratchpad cluster — `§4¶3` (also addressable as
`§4, post-scratchpad`).

## HTML comments

Section intro — `§5¶1`. HTML comments on their own lines act like blank
lines: they end the prior block but don't count as one.

<!-- single-line comment between two paragraphs -->

This paragraph is `§5¶2`.

<!--
  Multi-line
  comment that spans
  several lines
-->

And this is `§5¶3`.

## MDX components

Section intro — `§6¶1`. MDX/JSX components are one ¶ regardless of inner
content — the parser stays in MDX mode until the matching closer.

<Button label="Click me" onClick={() => alert('hi')} />

Self-closing `<Button />` above is `§6¶2`. This paragraph is `§6¶3`.

<Chart
  data={[1, 2, 3, 5, 8, 13]}
  title="Fibonacci"
  color="steelblue"
/>

Multi-line self-closing `<Chart />` above is `§6¶4`. This paragraph is
`§6¶5`.

<div className="callout">

**Note:** MDX lets you mix markdown *inside* JSX blocks. The inner
paragraphs, list, and blank lines inside this `<div>` all collapse into
a single `§6¶6` — the parser stays in MDX mode until the matching
`</div>`.

- inner list item one
- inner list item two

Closing prose inside the div.

</div>

After the `</div>`, this paragraph is `§6¶7`.

## Level-skipping (rare, discouraged)

Section intro — `§7¶1`. The next heading skips H3 and jumps directly to
H4. The path pads the missing H3 component with index `0`, which renders
as an empty middle segment: `§7..1`.

#### Skipped-H3 H4

The H4 line above is `§7..1`. This paragraph is `§7..1¶1`.

## Trailing notes and horizontal rules

Section intro — `§8¶1`.

---

The horizontal rule above is its own ¶ (`§8¶2`). This trailing paragraph
is `§8¶3`, and the file ends here.
