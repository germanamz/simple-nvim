# Feature Inventory & Test-Coverage Reference

**Date:** 2026-06-07
**Status:** Stage 1 deliverable (reference for the refactor)
**Companion:** `2026-06-07-refactor-design.md` (the actionable plan)

This is the exhaustive inventory of user-visible behaviors across the config —
90 features in 11 module clusters — produced by a 13-agent analysis pass. It is
the contract the refactor must preserve: every behavior below has, or needs, a
test that pins it before its module is touched (see the design doc's gap table).

Entrypoints (keymap / command / autocmd / public fn) are cited with line refs as
of this date; treat them as a map, not a guarantee — verify before relying.

## lua/config/telescope_smart.lua
- **Smart files picker with git-status awareness** (`smart-files-picker`): Opens telescope picker listing all files in the project, prefixed with single-letter git-status codes (A/M/D/R/?/b-prefix) with optional unstaged marker (*). Shows worktree changes vs index, and with a review base set, shows committed changes vs that base. — _M.smart_files() [line 514]; keybinding <leader><space> [plugins/telescope.lua:70-75]_
- **Changed files picker** (`smart-files-changed-picker`): Opens telescope picker listing only files with git changes (worktree or committed). Filters to files with non-empty git codes, sorted. Shows counts in legend. — _M.smart_files_changed() [line 531]; keybinding <leader>gs [plugins/telescope.lua:37-42]_
- **Git-status prefix rendering in all file pickers** (`git-status-prefix-rendering`): After setup(), all telescope file-listing pickers (find_files, oldfiles, smart pickers) display 2-character git-status prefix before each filename. Integrates via wrapping telescope.make_entry.gen_from_file. — _M.setup() [line 560]; Called in plugins/telescope.lua:124_
- **Legend window showing file status counts** (`status-legend-display`): On picker open, displays a floating window with counts of each status type (A/M/D/R/? in worktree, bA/bM/bD/bR vs review base). Positioned above results, auto-closes on picker close. — _open_legend(prompt_bufnr, counts, base) [line 364]; called from open_picker attach_mappings [line 501]_
- **Review base change support** (`review-base-integration`): When a review base is set (via M.pick in review_base module), files are classified as 'base-only' (committed changes vs base) with b-prefix, separate from worktree status. Cache is time-based (500ms TTL). — _M._git_changes(root, base) lines 181-212; refresh_codes cache read [line 264]; title generation in smart_files/smart_files_changed_
- **Fallback file listing (rg/fd/find)** (`fallback-file-listing`): When no git status available (e.g., non-repo), falls back to rg --files, then fd, then find to list all project files. Used in smart_files() for merge with git-tracked files. — _M._list_all() [line 217]; Called from smart_files() [line 517]_
- **Path deduplication and merging** (`path-deduplication`): Merges git-tracked files (with codes) and all files (from rg/fd/find), deduping by normalized path and prioritizing git-tracked entries first. — _M._merge_results(codes, all_files) [line 227]; Called from smart_files() [line 521]_

## markdown_paragraphs
- **Hierarchical Section Numbering** (`markdown-section-numbering`): Assigns dotted section paths (§1, §1.1, §1.2.3, etc.) to H2-H6 headings; H1 is ignored. Each heading level resets the count of deeper levels. Supports level-skipping (H2 -> H4 fills missing H3 with 0). — _M.attach(); M.compute() on TextChanged/TextChangedI; heading_level() at line 68; heading logic at lines 227-247_
- **Paragraph Numbering within Sections** (`markdown-paragraph-numbering`): Counts non-blank, non-structural blocks as paragraphs (¶1, ¶2, ...) within each section. Resets when a new heading is encountered. Blocks include: regular prose, code fences, blockquotes, lists (if blank-separated), and MDX components; scratchpad blockquotes are skipped. — _M.compute() block registration at lines 257-286; paragraph counter at lines 280-284, 263-266, 270-273_
- **YAML Frontmatter Handling** (`frontmatter-exclusion`): Detects and skips YAML frontmatter (---...---) at buffer start. All frontmatter lines ignored for section/paragraph numbering. Rejects unclosed or misplaced frontmatter. — _M.frontmatter_end(bufnr) at line 133; find_frontmatter_end() at line 121; M.compute() check at line 195_
- **Status Column Marker Rendering** (`markdown-statuscolumn-rendering`): Renders section and paragraph numbers (§1.2¶3) into status column with right-padding for column 81 alignment. Non-block lines show blank padding. — _M.marker() at line 331; _G._markdown_paragraph_marker global callback; STATUSCOLUMN at line 349; M.attach() applies window config at line 371_
- **Ruler Coloring and Luminance Adjustment** (`markdown-ruler-coloring`): Applies colorcolumn=81 with adaptive ColorColumn highlight. Blends toward black/white based on luminance detection. Re-applied on ColorScheme changes. — _apply_ruler_hl() at line 35, line 53, ColorScheme autocmd at line 54; blend() RGB interpolation at line 22_
- **Content Block Type Detection** (`markdown-block-classification`): Identifies block boundaries: code fences, HTML comments, MDX components, scratchpad blockquotes (Mental Note/TODO/etc.), and regular prose. Fenced and MDX blocks can span lines. — _is_code_fence() at line 63; is_html_comment_start() at line 84; has_html_comment_end() at line 88; is_scratchpad_first_line() at line 92; is_mdx_block_start() at line 103; mdx_balance() at line 107_
- **Setext Heading Detection and Skipping** (`markdown-setext-skipping`): Detects Setext-style headings (underlined with ==== or ----) and skips them from numbering. Requires preceding line to be text (prev_was_text flag). — _is_setext_underline() at line 80; setext branch at line 248_
- **Per-Window Configuration Management** (`markdown-window-config`): Attaches markdown mode features to buffer/window: sets statuscolumn, colorcolumn, marks window with markdown_writing_active flag. Detach removes customizations. — _M.attach() at line 351; M.apply_window() at line 374; M.detach_window() at line 380; M.get_starts() at line 389_

## gitsigns config: on_attach keymaps, custom new-vs-base line painter, global hunk-highlight toggle, GitSignsUpdate autocmd
- **Hunk navigation** (`hunk-navigation`): Navigate between hunks in the working tree using ]c (next) and [c (prev), with wraparound — _map('n', ']c', gs.nav_hunk('next'), 'Next hunk') at line 34-36; map('n', '[c', gs.nav_hunk('prev'), 'Prev hunk') at line 37-39_
- **Hunk preview** (`hunk-preview`): Show inline diff of current hunk or preview in floating window — _<leader>hp (gs.preview_hunk) at line 40; <leader>hi (gs.preview_hunk_inline) at line 49_
- **Stage/reset hunks** (`hunk-stage-reset`): Stage or reset individual hunks in the working tree — _<leader>hs (gs.stage_hunk) at line 41; <leader>hr (gs.reset_hunk) at line 42_
- **Blame line** (`blame-line`): Show blame for current line (full blame or line-only virtual text toggle) — _<leader>hb (gs.blame_line with full=true) at line 43-45; <leader>hB (gs.toggle_current_line_blame) at line 46_
- **Diff mode** (`diff-mode`): Open vimdiff against index or show deleted lines inline — _<leader>hd (gs.diffthis) at line 47; <leader>ht (gs.toggle_deleted) at line 48_
- **Review base diff** (`review-base-diff`): When a review base ref is set (e.g., origin/main), diff the buffer against that ref instead of index, detecting new-vs-base files — _apply_base(bufnr) in on_attach (lines 7-13, called at 29); User autocmd 'ReviewBaseChanged' (lines 64-70); file_new_vs_base(bufnr) detection at lines 102-121_
- **Custom extmark painter for hunks** (`custom-extmark-painter`): Paint line backgrounds and inline word-diff highlights for added/changed/deleted lines using extmarks (not gitsigns' built-in linehl). Handles new-vs-base files by painting entire buffer as 'add' — _GitSignsUpdate autocmd callback invokes repaint_all() (lines 302-308, callback 304-307); repaint_all() iterates all buffers and calls mark_hunks(buf) at lines 260-266; mark_hunks(bufnr) does custom extmark painting at lines 123-222; hunks_visible global flag gates hl_group assignment (line 142, 144)_
- **Toggle hunk highlights visibility** (`hunk-highlight-toggle`): Show/hide line background colors and inline word-diff via <leader>hh; hides backgrounds but keeps colored line numbers (numhl). Works even on untracked/new files where gitsigns never attaches — _<leader>hh global keymap at lines 298-300; _G.gitsigns_toggle_hunks() at lines 268-293; hunks_visible boolean state (line 95)_
- **Statusline hunk counts** (`statusline-hunk-counts`): Display hunk statistics (count by type + position relative to cursor) in statusline; stays live even when highlights are hidden — __G.gitsigns_hunks_status() function at lines 224-255; Called from statusline.lua to display ' +%d ~%d -%d ↑%d ↓%d ' format_

## lua/config/review_base.lua
- **Persist per-repo review base ref** (`persist-review-base`): Store and retrieve the review base (git ref) for each repository in a JSON file, with atomic write semantics using temp-file + rename — _M.set(root, ref); M.get(root); M.clear(root)_
- **Bootstrap: clean stale entries** (`bootstrap-stale-cleanup`): On startup, scan persisted state and remove entries whose git roots no longer exist or whose refs have been deleted — _M.bootstrap()_
- **Git utilities** (`git-utilities`): Resolve git root directories and verify refs exist in a repo — _M.git_root(start_path); M.resolve(root, ref)_
- **Broadcast ReviewBaseChanged event** (`user-event-broadcast`): Fire a User autocmd with data={root, ref} whenever the review base changes (set or clear) — _M.set(root, ref); M.clear(root); fire(root, ref)_
- **Telescope picker UI for review base selection** (`telescope-picker-ui`): Open interactive telescope picker showing available branches, highlight active base, preview commits, handle selection with validation and legend display — _M.pick(root, on_done)_
- **Legend display overlay** (`legend-display`): Show a floating window legend indicating the currently active review base, with highlights for active vs legend text — _open_legend(); close_legend()_

## lua/config/options.lua
- **Visual display settings** (`editor-opts-visual`): Line numbers, signcolumn, cursorline, whitespace rendering (tab/lead/trail chars), fillchars (eob marker). — _Module load triggers vim.opt assignments at lines 4-20_
- **Indentation config** (`editor-opts-indent`): 2-space expandtab with smartindent and softtabstop=2 — _Module load triggers vim.opt assignments at lines 23-27_
- **Search behavior** (`editor-opts-search`): Case-insensitive with smartcase, hlsearch, incsearch enabled — _Module load triggers vim.opt assignments at lines 30-33_
- **Miscellaneous UI options** (`editor-opts-ui-misc`): termguicolors, mouse support, unnamedplus clipboard, undo persistence, update/timeouts, split behavior, scrolloff, wrap/linebreak/breakindent, showbreak glyph — _Module load triggers vim.opt assignments at lines 67-106_
- **Toggle search highlight** (`keymap-search-toggle`): Keymap <leader>uh toggles hlsearch on/off (stays off until toggled back, unlike :nohlsearch) — _vim.keymap.set at line 37_
- **Delete buffer without closing window** (`keymap-buffer-delete`): Keymap <leader>bd switches to prev buffer, then wipes the old one, keeping window open — _vim.keymap.set at line 74_
- **Quit all windows (force)** (`keymap-quit-all-force`): Keymap <leader>qa quits all with :qa! — _vim.keymap.set at line 77_
- **Save all and quit** (`keymap-save-and-quit`): Keymap <leader>qw saves all and quits with :wqa — _vim.keymap.set at line 80_
- **OSC52 clipboard for containers/SSH** (`osc52-clipboard`): Detects container env (REMOTE_CONTAINERS, CODESPACES, /.dockerenv) or SSH_TTY, swaps vim.g.clipboard to use OSC52 protocol instead of native clipboard — _Conditional logic lines 83-93 (runs at module load if in_container or SSH_TTY)_
- **Force wrap in diff mode** (`diff-wrap-force`): Autocmd on OptionSet diff pattern fires when diff option toggles; if vim.v.option_new == '1', sets wrap=true and linebreak=true locally — _vim.api.nvim_create_autocmd at line 109, callback at lines 111-117_
- **Hide netrw tree bar** (`netrw-tree-bar-hide`): Sets netrw tree-style listing (g:netrw_liststyle=3), defines highlight netrwTreeBar to match Normal bg color so bar becomes invisible. Re-applied on ColorScheme to survive :colorscheme changes — _Module load calls hide_netrw_tree_bar() at line 63, vim.api.nvim_create_autocmd ColorScheme at line 64_
- **Restore netrw ? quickhelp binding** (`netrw-quickhelp`): Autocmd FileType netrw overwrites the bundled <F1>->quickhelp binding with <?>->quickhelp instead — _vim.api.nvim_create_autocmd at line 46, FileType pattern 'netrw', callback at lines 48-52_
- **Markdown text rewrap with prose/code handling** (`markdown-rewrap`): <leader>w in markdown/mdx buffers: rewraps prose to textwidth=80, skips table rows (lines starting with |), dispatches fenced code blocks through per-language formatters, preserves frontmatter and code block boundaries, restores cursor after rewrap — _vim.keymap.set at line 179 (markdown/mdx FileType autocmd), callback function lines 179-274_
- **Markdown buffer options** (`markdown-opts`): FileType markdown/mdx sets textwidth=80, appends formatoption 't' (auto-wrap text), disables soft-wrap (wrap=false for hard-wrapped prose), binds <leader>w, attaches paragraph numbering via markdown_paragraphs module — _vim.api.nvim_create_autocmd FileType at line 173, pattern ['markdown', 'mdx'], callback lines 175-276_
- **Markdown paragraph numbering window sync** (`markdown-paragraph-window`): Autocmd BufWinEnter applies/detaches paragraph number statuscolumn display based on current buffer filetype — _vim.api.nvim_create_autocmd BufWinEnter at line 279, callback lines 280-288_
- **Fenced code block formatting** (`fenced-code-format`): Internal function format_code_block(block) dispatches fence content through vim.system using formatter argv from config.formatters, preserves indentation, trims output, replaces buffer lines — _Called from markdown rewrap handler at line 264_
- **Fence line detection** (`fence-parse`): Internal function parse_fence(line) detects backtick or tilde fence markers, extracts language tag and indent, returns triple (indent, fence_string, lang) — _Called from markdown rewrap state machine at lines 204, 216_
- **Resolve formatter argv for fence language tag** (`formatter-resolve`): Calls require('config.formatters').resolve_fence_argv(block.lang) to look up shell command for a language's fenced code formatter — _format_code_block at line 124_
- **Attach paragraph numbering to markdown buffer** (`markdown-paragraphs-attach`): Calls require('config.markdown_paragraphs').attach(args.buf) to set up paragraph/section number tracking and statuscolumn display for a buffer — _markdown FileType autocmd callback at line 275_

## lsp_refs
- **Display reference count in statusline** (`reference-count-statusline`): Expose a global function _G.lsp_refs_status that returns a formatted reference count string for use in the statusline (e.g., ' ⇄3 '). Returns empty string when no references exist or cursor has moved off the cached symbol position. — _Function M.status() (line 192-203); Assigned to _G.lsp_refs_status in M.setup() (line 228); Status string rendered in statusline via Lua expression_
- **Detect and highlight in-buffer references via LSP** (`reference-detection-extmark`): On CursorHold/CursorHoldI, request textDocument/references from LSP. Dedup by (line, character) start position. Place extmarks (LspReferenceText highlight) for all in-buffer references when count >= 2. Clears marks if count < 2 or on cursor move away from symbol. — _Autocmd CursorHold / CursorHoldI (line 208-213), invokes request(args.buf); Function M.request(bufnr) (line 32-109), handles LSP callback and rendering_
- **Navigate forward to next reference** (`reference-navigation-forward`): Jump cursor to the next reference extmark in the current buffer, wrapping to the first mark if cursor is past the last reference. — _Function M.next() (line 185-187); Internal jump(1) (line 147-183, forward=1 branch)_
- **Navigate backward to previous reference** (`reference-navigation-backward`): Jump cursor to the previous reference extmark in the current buffer, wrapping to the last mark if cursor is before the first reference. — _Function M.prev() (line 188-190); Internal jump(-1) (line 147-183, backward=-1 branch)_
- **Invalidate state when cursor moves off symbol** (`reference-state-invalidation`): On CursorMoved/CursorMovedI, detect when cursor has left the cached symbol position and is not on any reference extmark. Clear state and extmarks, allowing re-detection when cursor holds on a new symbol. — _Autocmd CursorMoved / CursorMovedI (line 215-218), invokes invalidate_on_move(); Function invalidate_on_move() (line 126-143)_
- **Clean up state on buffer exit** (`reference-state-cleanup`): On BufLeave or BufWipeout, remove the buffer's state entry and clear all extmarks, preventing stale marks or state in other buffers. — _Autocmd BufLeave / BufWipeout (line 220-226), sets state[args.buf]=nil and calls clear_marks(args.buf)_

## lsp.lua + lsp-file-operations.lua + lsp_refs.lua (LSP stack: 190 LOC lsp.lua + 19 LOC lsp-file-operations.lua + 232 LOC lsp_refs.lua)
- **LSP Server Registration & Per-Filetype Gating** (`lsp-server-registration`): Registers 14 LSP servers (ts_ls, pyright, gopls, rust_analyzer, clangd, lua_ls, bashls, jsonls, yamlls, taplo, html, cssls, marksman, mdx_analyzer) via vim.lsp.config() and vim.lsp.enable(), with per-server filetypes list gating attachment. — _lsp.lua:7-53 (servers table definition); lsp.lua:141-167 (mason-lspconfig config: vim.lsp.config + vim.lsp.enable for each server)_
- **LSP Capabilities Merging (blink.cmp + file-operations)** (`lsp-capabilities-advertising`): Merges blink.cmp completion capabilities with nvim-lsp-file-operations' file-rename capabilities into each server's config before registration, advertising workspace/willRenameFiles and snippet/auto-import support. — _lsp.lua:158-162 (capabilities merge: file_ops_caps + blink.get_lsp_capabilities + cfg.capabilities)_
- **TypeScript GoToSourceDefinition Override** (`ts-goto-source-definition`): On LspAttach for ts_ls, overrides 'gd' keymap to use ts_ls's custom _typescript.goToSourceDefinition command (follows imports to real source), falling back to standard definition() if the command returns nothing. — _lsp.lua:63-75 (ts_goto_source_definition function); lsp.lua:96-102 (LspAttach callback: conditional 'gd' keymap)_
- **Buffer-Local LSP Keymaps (LspAttach)** (`lsp-keymap-registration`): Registers buffer-local keymaps on LspAttach: 'gd' (definition, overridden for ts_ls), ']r'/']r' (next/prev reference), '<leader>lr' (restart LSP on buffer). All silent, all buffer-scoped. — _lsp.lua:89-119 (LspAttach autocmd callback)_
- **LSP Reference Count & Navigation (]r / [r)** (`lsp-reference-navigation`): On CursorHold, requests LSP references, deduplicates in-buffer occurrences, highlights them with extmarks, displays count in statusline. ']r'/']r' keymaps jump between reference marks with wrap-around. State cleared on cursor move off marks or buffer exit. — _lsp.lua:104-106 (keymap registration: lsp_refs.next/prev); lsp_refs.lua:205-229 (M.setup: autocmd registration); lsp_refs.lua:185-190 (M.next/M.prev: public API)_
- **Diagnostic Float on CursorHold** (`diagnostic-float-on-cursorhold`): Registers a CursorHold autocmd that pops a floating window with the message of any diagnostic under the exact cursor position (scope='cursor'), allowing inline inspection without virtual text. Float closes when cursor moves. — _lsp.lua:83-87 (CursorHold autocmd: vim.diagnostic.open_float)_
- **LSP Client Restart on Buffer (<leader>lr)** (`lsp-client-restart`): Keymap '<leader>lr' stops all LSP clients attached to the current buffer and reloads the buffer with ':edit', forcing vim.lsp.enable to spin up fresh clients. Escape hatch for stale server state (e.g., case-only renames on case-insensitive filesystems). — _lsp.lua:112-117 (LspAttach callback: map '<leader>lr')_
- **Mason Tool Installer from Lock File** (`mason-tool-installer`): Reads mason-tool-versions.lock (JSON), extracts {name, version} pairs, passes to mason-tool-installer.setup(ensure_installed=...), auto-installing and pinning tool versions at startup. — _lsp.lua:170-189 (mason-tool-installer config: file read + decode + setup)_
- **MDX Analyzer Workspace File-Watch Capability Workaround** (`mdx-analyzer-capabilities`): mdx_analyzer registers a *.mdx glob watcher that Neovim 0.12's vim.glob rejects as invalid, crashing the server. Advertises didChangeWatchedFiles.dynamicRegistration=false to prevent client-side registration; points typescript lib to mason's bundled version. — _lsp.lua:39-52 (mdx_analyzer server config: capabilities + init_options)_
- **Lua LSP Runtime & Workspace Config** (`lua-lsp-runtime-config`): Configures lua_ls with LuaJIT runtime, vim global recognized, nvim runtime files in workspace library, telemetry disabled, and third-party checks disabled. — _lsp.lua:13-26 (lua_ls server config: settings.Lua)_

## statusline / formatters / conform
- **Git branch / review base status display** (`statusline-git-branch`): Shows current git branch name in statusline, falls back to gitsigns_head if not available, and appends review base if set (using ↗ arrow separator). — _vim statusline string (line 134): %{v:lua.git_branch_status()}; _G.git_branch_status() global function (lines 36-54); Triggered by buffer entry, write, or cwd change (autocmd lines 71-130)_
- **Statusline component initialization** (`statusline-setup`): Sets up global statusline format, caches git branch and review base per-buffer, clears netrw's window-local override, and listens for review base changes to refresh display. — _M.setup() function (lines 69-135); Autocmd groups: nvim_statusline (BufEnter, BufWritePost, DirChanged); FileType/BufWinEnter/BufEnter events for netrw handling; VimEnter event for initial buffer refresh; User ReviewBaseChanged autocmd for status refresh on base change_
- **Formatter selection by file type** (`formatters-by-filetype`): Maps Vim filetype strings (python, lua, go, etc.) to ordered lists of conform.nvim formatter names, used by conform for automatic selection and by markdown fence dispatch. — _M.by_ft table (lines 19-44); lua/plugins/conform.lua:23 reads for conform setup; lua/config/options.lua:124 reads for markdown fence dispatch_
- **Markdown code block formatter resolution** (`formatters-fence-argv`): Maps language tags (lua, python, js, etc.) to command-line argv for stdin/stdout formatter invocation; includes aliases for common tag variations (py→python, tsx→typescript, etc.); resolves to nil if binary not on PATH. — _M.resolve_fence_argv(tag) function (lines 96-107); M.fence_argv table (lines 47-68) for argv lookup; M.fence_aliases table (lines 71-91) for tag normalization; Called by lua/config/options.lua:124 in markdown fence format handler_
- **Format-on-save via conform.nvim** (`conform-format-on-save`): Configures conform.nvim to format buffers before write (BufWritePre, 500ms timeout), falls back to LSP for unsupported filetypes, and sets formatexpr globally for gq command. — _lua/plugins/conform.lua config() setup (lines 22-27); LspAttach autocmd (lines 31-39) to override formatexpr for conform-supported filetypes; <leader>F keymap (lines 41-43) for on-demand format with async=false_
- **Statusline plugin-owned hooks** (`statusline-stubs`): Pre-defines _G.lsp_refs_status and _G.gitsigns_hunks_status as no-op functions so the statusline format string is safe to evaluate before those plugins load; plugins override them once attached. — _Global function stubs (lines 58-67); Referenced in statusline format string (line 134); Overridden by lua/config/lsp_refs.lua:228 and lua/plugins/gitsigns.lua (separately)_

## treesitter (nvim-treesitter main branch API integration)
- **Parser installation and pinning** (`treesitter-parser-install`): Installs a fixed set of 26 treesitter parsers (markdown, typescript, lua, python, rust, etc.) at revisions pinned in tests/parser-revisions.lua, overriding nvim-treesitter's bundled revisions before install. — _lazy.nvim plugin config hook: lua/plugins/treesitter.lua:12 (config = function()); require('config.ts_pinned').apply() called at lua/plugins/treesitter.lua:49; require('nvim-treesitter').install(parsers) called at lua/plugins/treesitter.lua:52_
- **Parser language registration and aliasing** (`treesitter-language-alias`): Registers parser aliases for filetypes that don't have dedicated parsers: mdx uses markdown parser, jsonc uses json parser. Allows downstream plugins to resolve parser via global treesitter language registry. — _vim.treesitter.language.register('markdown', 'mdx') at lua/plugins/treesitter.lua:58; vim.treesitter.language.register('json', 'jsonc') at lua/plugins/treesitter.lua:60_
- **Automatic treesitter highlighting on FileType** (`treesitter-highlighting-autostart`): Attaches treesitter syntax highlighter to buffers with supported filetypes (lua, typescript, python, go, rust, c, cpp, yaml, toml, html, css, json, bash, vim, markdown, and 'react' variants). Triggered on BufRead and new files. Handles filetype-to-language mapping for mismatches (tsx→typescript, sh→bash, etc.). — _FileType autocmd with pattern=[list of 27 filetypes] at lua/plugins/treesitter.lua:99-111; vim.treesitter.start(buf, lang) called in callback at lua/plugins/treesitter.lua:104; ft_to_lang lookup table at lua/plugins/treesitter.lua:63-69 applied in callback at line 103_
- **Treesitter-based code folding** (`treesitter-folding`): Enables expression-based folding via treesitter for supported filetypes. Sets foldmethod=expr with vim.treesitter.foldexpr(). Also configures global fold defaults (foldlevel=99, foldlevelstart=99, foldcolumn='1') to keep folds open by default, and per-buffer indentexpr for nvim-treesitter indent handling. — _vim.wo.foldmethod set in callback at lua/plugins/treesitter.lua:106; vim.wo.foldexpr = 'v:lua.vim.treesitter.foldexpr()' at lua/plugins/treesitter.lua:107; vim.bo[buf].indentexpr = ... at lua/plugins/treesitter.lua:108; Global fold options (vim.opt.*) at lua/plugins/treesitter.lua:114-117_
- **Treesitter context visibility toggle** (`treesitter-context-toggle`): Provides <leader>ut keymap to toggle the treesitter-context UI (shows enclosing scope/function header). Plugin is lazy-loaded on BufReadPre/BufNewFile. — _<leader>ut keymap at lua/plugins/treesitter-context.lua:14-19; require('treesitter-context').toggle() called in keymap handler_
- **Jump to enclosing function/scope** (`treesitter-context-jump`): Provides [f keymap to navigate to the header of the enclosing function or control structure (e.g., class, if block). Respects vim.v.count1 for jumping N levels up. — _[f keymap at lua/plugins/treesitter-context.lua:21-26; require('treesitter-context').go_to_context(vim.v.count1) called in keymap handler_
- **Treesitter context plugin configuration** (`treesitter-context-config`): Configures treesitter-context behavior: max_lines=4 (show up to 4 lines of context), multiline_threshold=1, trim_scope='outer' (trim outer braces), mode='cursor' (show context at cursor position). — _lazy.nvim plugin opts at lua/plugins/treesitter-context.lua:5-11_
- **Parser revision pinning mechanism** (`ts-pinned-revision-override`): Core shim that overrides nvim-treesitter's bundled parser revisions with pinned SHAs/tags. Mutates parser_config[lang].install_info.revision before install() is called, allowing the installer to pull specific parser versions. Silently skips parsers not in the registry or without install_info. — _config.ts_pinned.apply(revisions_table) called at lua/plugins/treesitter.lua:49; Revisions loaded from tests/parser-revisions.lua at lua/plugins/treesitter.lua:50; M.apply function defined at lua/config/ts_pinned.lua:22-32_

## nvim-tree.lua
- **Auto-open nvim-tree on directory launch** (`directory-launch-auto-open`): When nvim is invoked with a directory argument (e.g., 'nvim .'), force-load nvim-tree during init so it displays instead of netrw. Stays lazy for file launches. — _init() function (lines 20-24): checks argc and argv[0] via vim.fn.isdirectory(), triggers lazy.load(); :NvimTreeToggle, :NvimTreeFindFile, :NvimTreeFindFileToggle commands_
- **Reveal current file in tree on toggle** (`reveal-current-file`): The <leader>e keymap opens/toggles the tree while revealing the current file being edited. Driven by update_focused_file option and NvimTreeFindFileToggle command. — _<leader>e keymap (lines 10-14): maps to :NvimTreeFindFileToggle<cr>; opts.update_focused_file.enable (line 31): enables tree expansion to track buffer focus_
- **Auto-close tree when file is opened** (`quit-on-file-open`): Closing tree after a file is opened from within the tree (quit_on_open option). Tree acts as an on-demand picker, not a persistent sidebar. — _opts.actions.open_file.quit_on_open = true (line 35); Driven by nvim-tree's built-in action logic, not custom code_
- **Close tree when Telescope picker opens** (`close-on-telescope-open`): Close the tree the moment any Telescope picker is launched. Prevents tree and picker from competing for focus/attention. — _User autocmd on TelescopeFindPre pattern (lines 62-69); Callback checks api.tree.is_visible() and calls api.tree.close()_
- **Display keyboard-hint winbar in tree** (`treebar-hint`): Pin a one-line help text to the top of the tree window (g? to see all mappings). Applied via TreeOpen event subscription. — _api.events.subscribe(api.events.Event.TreeOpen) callback (lines 53-58); Sets vim.wo[win].winbar using nvim_set_option_value()_

## Neovim config: completion (blink.cmp), which-key, and init.lua bootstrap
- **blink.cmp completion engine** (`blink-cmp-completion`): Prebuilt Rust fuzzy-matcher completion plugin with LSP, path, and buffer sources. Tab accepts, C-space opens menu, C-n/C-p navigate, C-e hides. Auto-shows documentation after 200ms. Signature help enabled. — _lazy.nvim plugin loader (completion.lua spec); LSP attach (capabilities advertised via mason-lspconfig.nvim)_
- **which-key leader key groups and LSP keymap labels** (`which-key-groups`): Keymap group definitions for <leader>b (buffer), <leader>f (find), <leader>g (git), <leader>k (keys), <leader>q (quit), <leader>u (toggle). Relabels Neovim 0.11 default gr* LSP keymaps (gra, grn, grr, gri, grt, gO) with human-readable descriptions. — _which-key.nvim plugin loader (which-key.lua spec); <leader>K keymap to show all keymaps_
- **Init.lua leader keymaps** (`leader-keymaps-init`): Two global keymaps: <leader>k? opens keybindings cheatsheet (docs/keybindings.md), <leader>E opens netrw file tree (Explore command). — _init.lua lines 32-34, 38_
- **Lazy.nvim bootstrap on fresh machine** (`lazy-nvim-bootstrap`): Clones lazy.nvim from GitHub to stdpath(data)/lazy if not present. On fresh install, runs `lazy.restore()` to snap all plugins to commits pinned in lazy-lock.json. — _init.lua lines 2-20, 40-47_
- **Global Vim settings (mapleader, maplocalleader, filetype)** (`global-vim-settings`): Sets <space> as mapleader, \ as maplocalleader. Registers .mdx as markdown filetype. — _init.lua lines 23-26_
- **Config module initialization** (`config-module-loading`): Requires and optionally sets up config.options, config.lsp_refs, config.statusline at Neovim startup. — _init.lua lines 28-30_
