; A type with its relations block, and a condition with its CEL body. A
; relation-less `type user` is one line and never folds (foldminlines).
[
  (type_declaration)
  (condition_declaration)
] @fold
